# Production Dependencies (Django only)
# This file is used by Railway for deployment.
# Full requirements.txt still exists for local dev with Streamlit.

# Database
psycopg2-binary==2.9.9

# Data processing (used by backend services)
pandas==2.1.4
openpyxl>=3.1.0

# Auth (MSAL for SharePoint sync in backend)
msal==1.26.0
bcrypt==4.1.2

# HTTP client (backend services)
requests==2.31.0

# Environment
python-dotenv==1.0.0

# Django framework
Django>=4.2,<5.0
dj-database-url>=2.1.0
whitenoise>=6.6.0
gunicorn>=21.0.0
